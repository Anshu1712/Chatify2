<!-- button_selector.xml -->
<selector xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Pressed state -->
    <item android:state_pressed="true" android:drawable="@drawable/press_btn" />
    <!-- Default state -->
    <item android:drawable="@drawable/defult" />
</selector>
